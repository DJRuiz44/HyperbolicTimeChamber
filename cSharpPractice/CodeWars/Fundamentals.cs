using System;

namespace CodeWars
{
    public static class ReturnNegative
    {
        public static int Run()
        {
            int num = 5;
            //Console.WriteLine(-Math.Abs(num));
            return -Math.Abs(num);
        }
    }
}